# apiVersion: v1
# kind: Service
# metadata:
#   name: hello
#   labels:
#     app: hello
# spec:
#   ports:
#     - port: 5000
#       targetPort: 5000
#   selector:
#     app: hello
#     tier: hello
#   type: LoadBalancer
apiVersion: v1
kind: Service
metadata:
  name: backend-load-balancer
spec:
  type: LoadBalancer
  ports:
  - port: 4000
    targetPort: 5000
  selector:
    app: backend
